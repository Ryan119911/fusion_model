# Compatibility config for checkpoints trained with the old 5D input.
train:
  seed: 42
  device: cuda

bbsmg:
  input_dim: 5
  feature_schema: stroke5_v0
  latent_dim: 128
  base_channels: 64
  out_channels: 1
  image_size: 128
  use_tanh: false
